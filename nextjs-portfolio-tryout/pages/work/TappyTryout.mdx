import { MyComponent, NewWindowLink, MyLayoutComponent, GithubLink } from './project-layout'

export const meta = {
  author: 'Rich Haines',
  date: 'MAR 09, 2021',
  tags: ['gatsbyjs', 'json'],
}

# printer board tryout

![](/assets/tappy_thumbnail.png)

### PURPOSE:

try to make a sample of non-intrusive mobile application testing.

## REFERENCES / REPOSITORIES:

<GithubLink repo={'louiscklaw/tapsterbot'}>louiscklaw/tapsterbot</GithubLink>

export default ({ children }) => <MyLayoutComponent meta={meta}>{children}</MyLayoutComponent>
