import { MyComponent, NewWindowLink, MyLayoutComponent, GithubLink } from './project-layout'

export const meta = {
  author: 'Rich Haines',
  date: 'MAR 09, 2021',
  tags: ['gatsbyjs', 'json'],
}

# printer board tryout

![](/assets/79d8c82649563e29b20c231b6bfd1efd.svg)

### PURPOSE:

My personal tryout for traefik API router.

### REFERENCES / REPOSITORIES:

<GithubLink repo={'louiscklaw/traefik-tryout'}>louiscklaw/traefik-tryout</GithubLink>

export default ({ children }) => <MyLayoutComponent meta={meta}>{children}</MyLayoutComponent>
