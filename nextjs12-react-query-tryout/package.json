{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "prettier_file": "prettier --write \"./pages/**/*.{js,jsx}\"",
    "gitUpdate": "git add . && git commit -m\"update nextjs12-react-query-tryout,\"",
    "init_dev": "yarn --dev && yarn dev",
    "clear": "rm -rf node_modules && rm -rf .next",
    "docker_rebuild": "docker compose up -d --build && docker compose logs -f",
    "into_docker": "docker compose exec -it next /bin/bash"
  },
  "dependencies": {
    "axios": "^0.26.1",
    "next": "latest",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-query": "^3.34.19"
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "puppeteer": "^11.0.0"
  }
}